<script>
  /*
  import Particles from "svelte-particles";
  import { loadSlim } from "@tsparticles/slim";

  let particlesInit = async (engine) => {
    await loadSlim(engine);
  };

  const options = {
    background: {
      color: {
        value: '#000000',
      },
    },
    fpsLimit: 60,
    interactivity: {
      events: {
        onHover: {
          enable: true,
          mode: 'repulse',
        },
        resize: {
          enable: true,
        },
      },
      modes: {
        repulse: {
          distance: 150,
          duration: 0.4,
        },
      },
    },
    particles: {
      color: {
        value: ['#00f3ff', '#ff00ff'],
      },
      links: {
        color: '#ffffff',
        distance: 150,
        enable: true,
        opacity: 0.1,
        width: 1,
      },
      move: {
        direction: 'none',
        enable: true,
        outModes: {
          default: 'out',
        },
        random: false,
        speed: 10,
        straight: false,
        trail: {
          enable: true,
          length: 10,
          fill: {
            color: '#000000',
          },
        },
      },
      number: {
        density: {
          enable: true,
          area: 800,
        },
        value: 30,
      },
      opacity: {
        value: 1,
      },
      shape: {
        type: 'circle',
      },
      size: {
        value: { min: 3, max: 5 },
      },
    },
    detectRetina: true,
  };
  */
</script>

<!-- <Particles id="tsparticles" class="absolute z-0 w-full h-full" {options} {particlesInit} /> -->
<div class="absolute z-0 w-full h-full bg-gradient-to-br from-gray-900 to-black opacity-50"></div>